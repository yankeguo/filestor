admin:
  username: admin
  password: REPLACE_ME
# Any S3-compatible object storage. Vendor examples:
#   Aliyun OSS:  endpoint: https://s3.oss-cn-hangzhou.aliyuncs.com, region: cn-hangzhou
#   Qcloud COS:  endpoint: https://cos.ap-guangzhou.myqcloud.com,   region: ap-guangzhou
#   AWS S3:      endpoint: https://s3.us-east-1.amazonaws.com,      region: us-east-1
#   MinIO:       endpoint: http://127.0.0.1:9000,                   region: us-east-1, force_path_style: true
s3:
  endpoint: https://s3.oss-cn-hangzhou.aliyuncs.com
  region: cn-hangzhou
  bucket: example-bucket
  access_key_id: REPLACE_ME
  secret_access_key: REPLACE_ME
  # force_path_style: false
upload:
  workspace: upload-workspace
# Optional OpenAI-compatible LLM endpoints. chat is used by file analysis
# on the upload page; chat.url and chat.model must be set together, effort is
# the model's reasoning effort, headers is an optional map of HTTP headers.
# embeddings is an independent endpoint (no chat defaults); embeddings.url and
# embeddings.model must be set together, dimensions is optional.
# llm:
#   chat:
#     url: https://api.example.com/v1/chat/completions
#     model: my-model
#     effort: medium
#     headers:
#       Authorization: Bearer REPLACE_ME
#   embeddings:
#     url: https://api.example.com/v1/embeddings
#     model: my-embedding-model
#     dimensions: 1024
#     headers:
#       Authorization: Bearer REPLACE_ME
